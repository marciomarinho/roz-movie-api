# Docker Compose Environment Configuration
# Copy this to .env.compose and modify as needed

# ============================================================================
# PostgreSQL Configuration
# ============================================================================
POSTGRES_DB=movie_api_db
POSTGRES_USER=movie_api_user
POSTGRES_PASSWORD=movie_api_password

# ============================================================================
# Keycloak Configuration
# ============================================================================
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=admin

# Keycloak Setup Script Configuration
KEYCLOAK_URL=http://keycloak:8080
KEYCLOAK_REALM=movie-realm
KEYCLOAK_CLIENT_ID=movie-api-client
KEYCLOAK_TEST_USERNAME=movieuser
KEYCLOAK_TEST_PASSWORD=moviepassword

# ============================================================================
# FastAPI Application Configuration
# ============================================================================
# Database connection (use service name 'db' for hostname)
DB_HOST=db
DB_PORT=5432
DB_NAME=movie_api_db
DB_USER=movie_api_user
DB_PASSWORD=movie_api_password

# Keycloak configuration (use service name 'keycloak' for hostname)
KEYCLOAK_ISSUER_URL=http://keycloak:8080/realms/movie-realm
KEYCLOAK_AUDIENCE=movie-api-client

# Python/FastAPI settings
PYTHONUNBUFFERED=1
LOG_LEVEL=INFO

# ============================================================================
# Notes for Production Deployment
# ============================================================================
# 1. Change all default passwords before deploying to production
# 2. Enable HTTPS/TLS for Keycloak
# 3. Use strong, randomly generated passwords
# 4. Store sensitive values in a secrets manager
# 5. Configure CORS to allow only specific origins
# 6. Add resource limits to containers
# 7. Set up centralized logging and monitoring
# 8. Configure database backups
